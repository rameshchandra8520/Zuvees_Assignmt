/**
 * @param { import("knex").Knex } knex
 * @returns { Promise<void> } 
 */
exports.seed = async function(knex) {
  // Products will be created through the admin panel
  // This seed file is kept for database initialization purposes
  console.log('Products seed file executed - products will be created through admin panel');
};
