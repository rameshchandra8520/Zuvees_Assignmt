/**
 * @param { import("knex").Knex } knex
 * @returns { Promise<void> } 
 */
exports.seed = async function(knex) {
  // Product variants will be created through the admin panel
  // This seed file is kept for database initialization purposes
  console.log('Product variants seed file executed - variants will be created through admin panel');
};
